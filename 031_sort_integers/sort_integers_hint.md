# Sort 3 Integers - Hint

## Algorithm
- Find minimum using `min(a, b, c)`
- Find maximum using `max(a, b, c)`
- Find middle: `total_sum - minimum - maximum`

## Note
The middle value can be found by subtracting the min and max from the total sum.