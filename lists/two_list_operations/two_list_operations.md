# Two-List Operations

Given two lists of equal length, create a third list containing the sum of corresponding elements.

## Task

- Create a function `two_list_sum(list1, list2)` that takes two lists of integers
- Return a new list where each element is the sum of corresponding elements from both lists

## Template:

```python
def two_list_sum(list1: list[int], list2: list[int]) -> list[int]:
    pass


if __name__ == "__main__":
    # Test your function
    print(two_list_sum([1, 2, 3], [4, 5, 6]))  # [5, 7, 9]
    print(two_list_sum([10, 20], [5, 10]))     # [15, 30]
```

## Note

- Assume both lists have the same length
