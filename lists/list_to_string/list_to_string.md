# List to String

Create a function to convert a list of words into a single sentence string.

## Template:

```python
def list_to_string(words: list[str]) -> str:
    pass


if __name__ == "__main__":
    # Test your function
    print(list_to_string(["Hello", "world"]))  # "Hello world"
    print(list_to_string(["Python", "is", "fun"]))  # "Python is fun"
    print(list_to_string([]))  # ""
```

## Note

- The function should return an empty string for an empty list
