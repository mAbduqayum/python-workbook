# Next Rounds

Track the next rounds in a card game tournament.

## Task

- Create a function `next_rounds(n)` that takes the current round number
- Return a list containing the next three rounds

## Template:

```python
def next_rounds(n: int) -> list[int]:
    pass


if __name__ == "__main__":
    # Test your function
    print(next_rounds(27))  # [28, 29, 30]
    print(next_rounds(0))  # [1, 2, 3]
```
