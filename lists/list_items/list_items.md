# List Items

Create three functions to get the first, middle, and last items from a list.

## Template:

```python
def first_item(items):
    pass


def mid_item(items):
    pass


def last_item(items):
    pass


if __name__ == "__main__":
    # Test your functions
    items = [2, 3, 5, 7, 11]
    print(first_item(items))  # 2
    print(mid_item(items))    # 5
    print(last_item(items))   # 11
```

## Note

- For lists with even length, `mid_item` returns the second of the two middle items
- Assume the list is not empty
